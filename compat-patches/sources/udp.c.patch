diff --git a/drivers/net/ovpn/udp.c b/drivers/net/ovpn/udp.c
index 493a5a0..8f16045 100644
--- a/drivers/net/ovpn/udp.c
+++ b/drivers/net/ovpn/udp.c
@@ -13,6 +13,7 @@
 #include <linux/udp.h>
 #include <net/addrconf.h>
 #include <net/dst_cache.h>
+#include <net/ip6_route.h>
 #include <net/route.h>
 #include <net/transp_v6.h>
 #include <net/udp.h>
@@ -196,9 +197,9 @@ static int ovpn_udp4_output(struct ovpn_peer *peer, struct ovpn_bind *bind,
 	dst_cache_set_ip4(cache, &rt->dst, fl.saddr);
 
 transmit:
-	udp_tunnel_xmit_skb(rt, sk, skb, fl.saddr, fl.daddr, 0,
-			    ip4_dst_hoplimit(&rt->dst), 0, fl.fl4_sport,
-			    fl.fl4_dport, false, sk->sk_no_check_tx, 0);
+	ovpn_udp_tunnel_xmit_skb(rt, sk, skb, fl.saddr, fl.daddr, 0,
+				 ip4_dst_hoplimit(&rt->dst), 0, fl.fl4_sport,
+				 fl.fl4_dport, false, sk->sk_no_check_tx);
 	ret = 0;
 err:
 	local_bh_enable();
@@ -250,7 +251,7 @@ static int ovpn_udp6_output(struct ovpn_peer *peer, struct ovpn_bind *bind,
 		dst_cache_reset(cache);
 	}
 
-	dst = ip6_dst_lookup_flow(sock_net(sk), sk, &fl, NULL);
+	dst = ovpn_ipv6_dst_lookup_flow(sock_net(sk), sk, &fl, NULL);
 	if (IS_ERR(dst)) {
 		ret = PTR_ERR(dst);
 		net_dbg_ratelimited("%s: no route to host %pISpc: %d\n",
@@ -271,9 +272,9 @@ static int ovpn_udp6_output(struct ovpn_peer *peer, struct ovpn_bind *bind,
 	 * udp_tunnel_xmit_skb()
 	 */
 	skb->ignore_df = 1;
-	udp_tunnel6_xmit_skb(dst, sk, skb, skb->dev, &fl.saddr, &fl.daddr, 0,
-			     ip6_dst_hoplimit(dst), 0, fl.fl6_sport,
-			     fl.fl6_dport, udp_get_no_check6_tx(sk), 0);
+	ovpn_udp_tunnel6_xmit_skb(dst, sk, skb, skb->dev, &fl.saddr, &fl.daddr,
+				  0, ip6_dst_hoplimit(dst), 0, fl.fl6_sport,
+				  fl.fl6_dport, udp_get_no_check6_tx(sk));
 	ret = 0;
 err:
 	local_bh_enable();
@@ -399,7 +400,8 @@ int ovpn_udp_socket_attach(struct ovpn_socket *ovpn_sock, struct socket *sock,
 	if (!old_data) {
 		/* socket is currently unused - we can take it */
 		rcu_read_unlock();
-		setup_udp_tunnel_sock(sock_net(ovpn_sock->sk), sock->sk, &cfg);
+		ovpn_setup_udp_tunnel_sock(sock_net(ovpn_sock->sk), sock->sk,
+					   &cfg);
 		return 0;
 	}
 
@@ -436,12 +438,12 @@ void ovpn_udp_socket_detach(struct ovpn_socket *ovpn_sock)
 	struct sock *sk = ovpn_sock->sk;
 
 	/* Re-enable multicast loopback */
-	inet_set_bit(MC_LOOP, sk);
+	ovpn_udp_set_mc_loop(sk);
 	/* Disable CHECKSUM_UNNECESSARY to CHECKSUM_COMPLETE conversion */
 	inet_dec_convert_csum(sk);
 
 	WRITE_ONCE(udp_sk(sk)->encap_type, 0);
-	WRITE_ONCE(udp_sk(sk)->encap_rcv, NULL);
+	ovpn_udp_clear_encap_err_rcv(sk);
 	WRITE_ONCE(udp_sk(sk)->encap_destroy, NULL);
 
 	rcu_assign_sk_user_data(sk, NULL);
